# Database - PostgreSQL (Neon.tech, Supabase, or your own host)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
DATABASE_URL="postgresql://user:password@ep-example-123456.us-east-2.aws.neon.tech/coachflow?sslmode=require"

# Session secret for iron-session (must be at least 32 characters)
SESSION_SECRET="change_this_to_a_random_string_at_least_32_chars"

# Application URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"
